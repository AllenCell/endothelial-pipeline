[tool.ruff]
# Extend the rules from main `pyproject.toml` file
extend = "../../../../pyproject.toml"

[tool.ruff.lint]
ignore = [
    "D100", # Undocumented public module
    "D104", # Undocumented public package
    "D105", # Missing docstring in magic method
    "D107", # Missing docstring in __init__
    "D202", # Blank lines between the function body and the function docstring
    "D203", # 1 blank line required before class docstring
    "D205", # 1 blank line required between summary line and description
    "D212", # Multi-line docstring summary should start at the first line
    "D213", # Multi-line docstring summary should start at the second line
    "D413", # Missing blank line after last section
    "D416", # Section name should end with a colon
    "I001", # unsorted-imports
    "B023", # function-uses-loop-variable
    "B035", # static-key-dict-comprehension (these errors are probably bugs)
    "F821", # undefined-name (these errors are bugs)
]
