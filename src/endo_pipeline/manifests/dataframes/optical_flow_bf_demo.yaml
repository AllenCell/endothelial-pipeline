name: optical_flow_bf_demo
workflow: compute_optical_flow_feats
parameters:
  channel: BF
  level: 1
  max_dt: 1
  flow_scope: image
  intensity_percentile: 0
  attachment: 2.5
  compute_block_coherence: false
  annotations_excluded: [auto_bf_scope_error, auto_bf_temp_artifact, auto_gfp_scope_error,
    bf_scope_error, bf_temp_artifact, cell_piling, gfp_scope_error, not_steady_state,
    unfed, xy_shift, z_shift]
locations: {}
