import numpy as np

# miscellanous input parameters for fitting model
N = (32,32) # number of grid points in each dimension (int if 1D, tuple if 2D)

nf = 3 # highest order of the polynomial terms in SINDy library for drift (int)
ns = 1 # highest order of the polynomial terms in SINDy library for diffusion (int)
